## Build

### Use in AI Studio (recommended）

This contains everything you need to run your app locally.
View this app in AI Studio: https://ai.studio/apps/drive/1fzpseLjezmFDP9R7EL1IxBfHay53Sqfg
Anyone with a Google account can use it free.

### Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
